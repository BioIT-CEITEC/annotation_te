import os
import pandas as pd
import json
from snakemake.utils import min_version

min_version("5.18.0")

GLOBAL_REF_PATH = "/mnt/references/"

##### Config processing #####
# Folders
#
reference_path = os.path.join(GLOBAL_REF_PATH,"general","TE_db",config["reference"])

##### Target rules #####
rule all:
    input:  expand("mapped/{sample}.bam",sample = sample_tab.sample_name),
            expand("mapped/{sample}.bam.bai", sample = sample_tab.sample_name),

##### Modules #####

include: "rules/run_reasonate.smk"


